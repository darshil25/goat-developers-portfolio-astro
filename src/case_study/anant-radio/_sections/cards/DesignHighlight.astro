---
// DesignHighlight.astro
import { Image } from 'astro:assets';
---

<div class="design-highlight">
  <div class="card flex items-center">
    <div class="content flex flex-col">
      <h3 class="font-thin leading-tight text-[#fbfbfb] text-xl">
        Design Highlights
      </h3>
      <div class="content-custom flex flex-col text-[#fbfbfb] text-xs font-light leading-tight">
        <p>
          Our UX team worked in sprints using the Design Thinking framework.
          After mapping clinic workflows, we designed interfaces for every
          user type: radiologists, technicians, admins, and reception staff.
        </p>
        <p class="text-sm font-normal">Key UX Wins:</p>
        <ul class="flex flex-col">
          <li>Realtime dashboards for patients, queues, and scans</li>
          <li>Color-coded patient states and quick actions</li>
          <li>Role-based UIs for staff (Doctor, Admin, Receptionist)</li>
          <li>Dark/light mode and low-bandwidth optimization</li>
          <li>Mobile-friendly design for clinic staff on the go</li>
        </ul>
        <p>
          We focused on clarity, hierarchy, and speed — interfaces that
          reduce clicks, minimize errors, and work in low-connectivity
          clinics.
        </p>
      </div>
    </div>
    <div class="image flex items-center justify-center">
      <Image
        src="https://anantradio.blr1.cdn.digitaloceanspaces.com/goatdevelopers/anant-radio/aradio-design-grid-7.png"
        alt="aradio-challenge"
        width={417}
        height={314}
      />
    </div>
  </div>
</div>

<style>
  /* Card Styles */
  .card {
    gap: 80px; /* Specific override for design highlight */
  }

  .card .image {
    margin: 0 auto;
    padding: 24px;
    border-radius: 10px;
    background: #100f13;
    background-image: url(https://anantradio.blr1.cdn.digitaloceanspaces.com/goatdevelopers/anant-radio/aradio-case-study-card-bg.svg);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .card .image img {
    max-width: 417px;
    aspect-ratio: 1.328;
    border-radius: 10px;
    width: auto;
    height: auto;
  }

  .card .content {
    gap: 12px;
  }

  .content-custom {
    gap: 8px;
  }

  .content-custom ul {
    gap: 12px;
  }

  .content-custom ul li {
    list-style: disc;
    margin-left: 16px;
  }

  /* Media Queries */
  @media (min-width: 1260px) {
    .design-highlight {
      width: 1080px;
      margin: 0 auto;
    }
  }

  @media (min-width: 1023px) and (max-width: 1260px) {
    .design-highlight {
      max-width: 80%;
      margin: 0 auto;
    }
  }

  @media (min-width: 700px) and (max-width: 1023px) {
    .design-highlight {
      max-width: 90%;
      margin: 0 auto;
    }
    .card {
      gap: 60px;
      flex-direction: column;
    }
    .card .content {
      max-width: 465px;
    }
  }

  @media (max-width: 700px) {
    .design-highlight {
      width: 90%;
      margin: 0 auto;
    }
    .card {
      width: 100%;
      flex-direction: column;
    }
    .card .image {
      width: 100%;
      max-width: 400px;
      margin: 0 auto;
      padding: 14px;
      border-radius: 7px;
    }
    .card .image img {
      max-width: 278px;
      border-radius: 7px;
    }
    .card .content {
      gap: 24px;
    }
  }
</style>