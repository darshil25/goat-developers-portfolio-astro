---
// components/Details.astro
---

<div class="details text-[#fbfbfb] relative flex justify-between items-start">
  
  <div class="details-1">
    <div>
      <p>Industry</p>
      <p class="whitespace-nowrap">AI & Creative Tools</p>
    </div>
    <div>
      <p>Duration</p>
      <p>6 Week</p>
    </div>
  </div>

  <div class="details-2">
    <div class="details-2-1">
      <div>
        <p>Project Type:</p>
        <p>Internal Tool – B2B SaaS (B2C In future)</p>
      </div>
      <div>
        <p>Platform Type</p>
        <p>Web Application (React + Notion-like editor)</p>
      </div>
    </div>
    <div class="details-2-2">
      <p>Scope:</p>
      <p>UX Design + Frontend Development (Module only)</p>
    </div>
  </div>

  <div class="details-3">
    <p>Status:</p>
    <ul>
      <p>
        Design & Frontend Complete (Under active development by client team)
      </p>
    </ul>
  </div>

  <div class="bg-gradient absolute top-1/2 -translate-y-1/2 left-1/2 -translate-x-1/2">
    {Array.from({ length: 5 }).map(() => (
      <span />
    ))}
  </div>
</div>

<style>
  /* --- Base Styles (Desktop / Default) --- */
  
  .details {
    gap: 40px;
    /* Font color is handled by utility class text-[#fbfbfb] in HTML, or add here: color: #fbfbfb; */
  }

  /* Section 1 Styling */
  .details-1 {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 40px;
  }

  /* Section 2 Styling */
  .details-2 {
    display: flex;
    gap: 40px;
    width: 40%;
    align-items: flex-start;
  }

  .details-2-1 {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .details-2-2 {
    gap: 8px;
  }
  
  /* List Styling within details-2-2 (if ul exists there) */
  .details-2-2 ul {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  
  .details-2-2 ul li {
    list-style: disc;
    margin-left: 16px;
    padding-inline-start: 10px;
    text-indent: -16px;
  }

  /* --- Glass Card Styling (Shared across all inner divs) --- */
  .details-1 > div,
  .details-2-1 > div,
  .details-2-2,
  .details-3 {
    display: flex;
    flex-direction: column;
    padding: 8px 16px;
    background-color: rgba(255, 255, 255, 0.01);
    border-radius: 7px;
    backdrop-filter: blur(12px);
  }

  /* Label Styling (First Child) */
  .details-1 > div > :first-child,
  .details-2-1 > div > :first-child,
  .details-2-2 > :first-child,
  .details-3 > :first-child {
    font-size: 12px;
    line-height: 1.2;
    opacity: 0.5;
  }

  /* Value Styling (Last Child) */
  .details-1 > div > :last-child,
  .details-2-1 > div > :last-child,
  .details-2-2 > :last-child,
  .details-3 > :last-child {
    font-size: 14px;
    line-height: 1.25;
  }

  /* --- Gradient Background --- */
  .bg-gradient {
    display: flex;
    opacity: 0.2;
    filter: blur(58.89637756347656px);
    z-index: -1; /* Ensure it stays behind */
    pointer-events: none;
  }

  .bg-gradient span {
    border-radius: 100%;
    display: flex;
    width: 176px;
    height: 165px;
    background: white;
  }

  .bg-gradient span:not(:first-child) {
    margin-left: -47px;
  }

  /* --- MEDIA QUERIES --- */

  /* > 1281px */
  @media (min-width: 1281px) {
    .details {
      max-width: 1080px;
      margin: 0 auto;
    }
  }

  /* 1259px - 1440px */
  @media (min-width: 1259px) and (max-width: 1440px) {
    .details {
      max-width: 1080px;
      margin: 0 auto;
    }
  }

  /* 1023px - 1259px */
  @media (min-width: 1023px) and (max-width: 1259px) {
    .details {
      margin: 0 auto;
      max-width: 90%;
    }
    .details-2 {
      width: 65%;
    }
  }

  /* 700px - 1023px */
  @media (min-width: 700px) and (max-width: 1023px) {
    .details {
      margin: 0 auto;
      max-width: 80%;
      flex-direction: column;
    }
    
    .details-1 {
      flex-direction: row;
    }
    
    .details-2 {
      width: auto;
    }
  }

  /* < 700px */
  @media (max-width: 700px) {
    .details {
      padding: 0 16px;
      gap: 12px;
      flex-direction: column;
    }

    .details-1 {
      gap: 12px;
      flex-direction: row;
    }

    .details-2 {
      gap: 12px;
      width: auto;
    }

    .details-2-1 {
      gap: 12px;
    }

    .details-2-2 {
      gap: 4px;
    }

    .bg-gradient span:not(:first-child) {
      margin-left: -117px;
    }
  }
</style>