---
// components/section2/Objectives.astro
import { Image } from 'astro:assets';
import Ellipse from "@/components/ui/reusable/vs-ellipse.astro";

import image from '@/assets/rawfiles/vanquish-solutions/vanquish-objective.png'

---

<section class="container mx-auto px-4 py-12 md:py-24">
  <div class="w-full flex flex-col md:flex-row items-center justify-between gap-10 md:gap-20">
    
    <div class="flex flex-col gap-6 w-full md:w-5/12">
      <h2 class="section-title text-center md:text-left">Objectives</h2>
      
      <div class="card-container relative overflow-hidden bg-[#100f13] p-6 md:p-8 rounded-2xl">
        <ul class="flex flex-col gap-4 text-[#cfcfcf]">
          <li>
            Reimagine copytrading UX for mobile-first users.
          </li>
          <li>
            To Replace old Software.
          </li>
          <li>
            To Build In-House Software for vertical integration and self-reliance to better position in long term.
          </li>
          <li>
            New, Better, Modern, Minimal Design language to replace old one.
          </li>
        </ul>
        
        <Ellipse class="custom-ellipse" />
      </div>
    </div>

      <div class="image-wrapper">
        <Image 
          src={image} 
          alt="App Strategy Interface" 
          class="object-contain w-full h-auto max-h-[500px]"
        />
      </div>

  </div>
</section>

<style>
  .section-title {
    color: #444; 
    font-size: 20px;
    font-weight: 400;
    text-transform: capitalize;
  }

  ul li {
    list-style: disc;
    margin-left: 20px;
    font-size: 15px;
    line-height: 1.6;
    font-weight: 300;
    color: #e5e5e5;
  }

  /* The Card Background */
  .card-container {
    border: 1px solid rgba(255, 255, 255, 0.03);
    box-shadow: 0 20px 40px rgba(0,0,0,0.4);
  }

  /* Navigation Arrows */
  .nav-arrow {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: #0a0a0a;
    border: 1px solid #222;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #666;
    cursor: pointer;
    transition: all 0.2s ease;
    z-index: 10;
    position: absolute;
  }

  .nav-arrow:hover {
    background-color: #222;
    color: white;
  }

  /* Image Caption */
  .image-caption {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 12px;
    color: #666;
    opacity: 0.8;
  }

  .image-wrapper {
    position: relative;
    width: 100%;
    display: flex;
    justify-content: center;
  }

  .card-container :global(.custom-ellipse) {
    position: absolute;
    top: 60%;
    left: 20%;
    width: 100%; /* Wider spread */
    height: 150px;
    transform: rotate(-10deg);
    background: radial-gradient(circle, rgba(255,255,255,0.08) 0%, rgba(0,0,0,0) 70%);
    filter: blur(40px);
    pointer-events: none;
    z-index: 0;
  }

  @media (max-width: 768px) {
    ul li {
      font-size: 13px;
      line-height: 1.5;
    }
  }
</style>